# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently in the **planning phase**. It contains only a `README.md` describing "LaunchPad", a planned free/open-source GUI tool for managing macOS `launchd` services.

### Current State

- No source code, build system, or dependencies exist yet.
- The tech stack is undecided (Swift+SwiftUI, Tauri+React, or Electron are under consideration).
- There are no services to start, no tests to run, and no linting configured.

### When Code is Added

Once a tech stack is chosen and code is added, this section should be updated with:
- How to install dependencies (update script should be adjusted accordingly).
- How to run the development server.
- How to run tests and linting.
- Any non-obvious setup caveats discovered during development.
